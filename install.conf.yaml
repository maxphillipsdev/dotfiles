- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.config/lvim: config/lvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [brew bundle install]
